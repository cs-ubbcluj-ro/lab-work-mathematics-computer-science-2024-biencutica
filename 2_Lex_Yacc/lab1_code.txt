#include <stdio.h>
using namespace std;

int main() {
	int a, b, max;
	cin >> a;
    cin >> b;
	if (a > b){
		max = a;
    }
	else{
		if(a == b){
			max = 0;
        }
		else{
			max = b;
        }
    }

	cout << max;
	return 0;
}