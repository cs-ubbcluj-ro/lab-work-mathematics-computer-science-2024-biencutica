#include <stdio.h>
using namespace std;

struct Person {
    int age;
    float height;
};

int main() {
    int a, b;

    a = 10;
    b = 20.3;

    if (a < b) {
        cin >> a;
        cout << a;
    } else {
        return 0;
    }

    while (b > a) {
        b = b - 1;
    }

    return 0;
}
